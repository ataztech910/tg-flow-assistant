# BotFlow — бэклог

Живой список того, чего не хватает. Отмечаем `[x]` по мере закрытия, не удаляем закрытое —
чтобы видно было, что уже проверено.

## Сейчас (закрыто — все три пункта готовы)

- [x] **Виртуальный прогон флоу (как test run в n8n) — визуальный** — уточнение: имелся в виду
      прогон поверх диаграммы, не терминал. Панель "Test run" на `/bots/:id` управляет тем же живым
      `FlowEngine`, что обслуживает реального бота, через зарезервированный `userId = -1`
      (реальные Telegram id всегда положительные). Активный узел подсвечивается и в него
      автоматически центрируется вид (`useReactFlow().fitView`) — при заходе на страницу это же
      сразу показывает `start_node`, не только во время прогона. `webhook`/`collect` выполняются
      по-настоящему, платёж — по кнопке "Simulate successful payment". `test-run.ts` (терминал)
      остаётся отдельным headless-инструментом, не заменён.
- [x] **Визуальная проверка флоу (Mermaid)** — `mermaid.ts`: детерминированный конвертер
      `flow.yaml → Mermaid`, разные фигуры по типам узлов, пунктир для `on_error`/`on_fail`.
      Проверено на демо-флоу, опубликовано как артефакт.
- [x] **Процесс создания бота (визуальный)** — `server.ts` + `bot-manager.ts` + `views.ts`:
      HTTP-дашборд, форма (токен + flow.yaml) → валидация (`schema.ts` + `flow-check.ts` + реальный
      `getMe`) → `bots/<id>/{meta.json,flow.yaml}` → бот стартует в этом же процессе. `/bots/<id>` —
      живая Mermaid-диаграмма (та же `flowToMermaid` из `mermaid.ts`) + последние собранные данные +
      Start/Stop. При рестарте сервера все боты из `bots/` поднимаются автоматически.
      Нашли и починили по пути: `bot.start()` с невалидным/отозванным токеном кидал необработанную
      ошибку и ронял **весь процесс** (все боты разом) — теперь каждый запуск изолирован
      (`BotManager.launch`), плохой токен просто помечает своего бота "stopped".
      **Известный гэп:** токен в `meta.json` хранится в открытом виде — ок для локального POC,
      шифрование — часть будущей фазы Auth.

## Дальше — AI-редактор флоу в дашборде (Tiptap AI Toolkit-style)

- [x] **Reload flow без рестарта** — `bot-manager.ts`: `BotManager.reloadFlow(id)` перечитывает
      `bots/<id>/flow.yaml` с диска, валидирует (`assertValidFlow` — общий хелпер, теперь и
      `createBot`, и `reloadFlow` используют одну и ту же проверку), применяет на уже работающий
      `engine.loadFlow()` — без даунтайма остальных ботов. Кнопка "↻ Reload flow" на дашборде.
      Ближайший практический workflow: правки в `flow.yaml` делаются через Claude Code напрямую
      (см. ниже), затем жмётся эта кнопка.
- [x] **Версионирование флоу** — `bot-manager.ts`: `saveFlow`/`archiveCurrentFlow`/`listVersions`/
      `restoreVersion`. Любое принятое изменение архивирует текущий `flow.yaml` в
      `bots/<id>/versions/<ts>__<label>.yaml` перед перезаписью; невалидные правки отклоняются без
      создания версии; restore сам архивируется (откат обратим). Секция "Version history" на
      дашборде. Проверено end-to-end через реальные HTTP-запросы к серверу.
- [x] **Точки входа/выхода + форма свойств узла (n8n-style)** — клик по карточке на графе (не
      драг) открывает панель с полями конкретного типа узла (зеркалят `schema.ts`), включая
      редактируемый список кнопок, "Set as start" и "Delete node". "+ Add node" с выбором типа.
      Сохранение шлёт весь объект `flow` на `POST /bots/:id/flow` → `saveFlow`. Удаление узла, на
      который остались ссылки, корректно отклоняется валидатором (не пытаемся тихо чинить сами).
      Известное ограничение: `headers`/`body`/`save_response_as` у `webhook` пока не редактируются
      формой (только текстом/AI) — заметно небольшой процент случаев, не блокер.
- [x] **Чат-редактор флоу (как Tiptap AI Toolkit)** — `ai-editor.ts` + вкладка "Ask AI" в панели
      узла. Зовёт `@kitana-sdk/core` (роутер `chain: ['claude','codex','ollama','api-key']`,
      по умолчанию бесплатно через `claude auth login`, фоллбэк на `ANTHROPIC_API_KEY`/
      `OPENAI_API_KEY`). Промпт = `dsl-rules.md` + текущий `flow.yaml` + (если узел выбран) на
      каком узле фокус + инструкция пользователя; модель отвечает только текстом (Kitana отключает
      ей все инструменты), мы сами валидируем ответ через `schema.ts`+`flow-check.ts` с одним
      автоматическим повтором (текст ошибки скармливается обратно), затем `saveFlow` (версия
      архивируется как обычно).
      **Проверено вживую, реальным вызовом:** инструкция "Add a new message node called pricing...
      and add a button on welcome" → Claude через `claude -p` (~7 сек) → корректный YAML с новым
      узлом и кнопкой, прошёл валидацию с первого раза, сохранился, версия заархивирована.
      `deno.json` теперь в проекте (`deno add npm:@kitana-sdk/core`), нужен `--allow-run` при
      запуске `server.ts`.

## Дальше — сразу после диаграммы

- [x] **React Flow вместо Mermaid** — `/bots/:id` теперь рендерит интерактивный, перетаскиваемый
      граф вместо статичной Mermaid-картинки. `flow-to-reactflow.ts` считает layout на сервере
      (BFS-глубина от `start_node` → колонка, без dagre/elk), отдаёт nodes/edges как JSON;
      страница поднимает React+ReactDOM (cdnjs) + reactflow UMD (jsdelivr) без сборки/JSX.
      Проверено на реальном demo-флоу (12 узлов/16 рёбер) через изолированный рендер-скрипт —
      живьём в браузере ещё не смотрели, нужно перезапустить `server.ts` и открыть `/bots/<id>`.
      `mermaid.ts` оставался как отдельный CLI-инструмент, но при аудите мёртвого кода перед
      опенсорсом выяснилось, что им никто не пользуется (дашборд полностью на React Flow) —
      удалён, см. "Уже закрыто".
- [ ] **Автообогащение `collect` телеграм-профилем** — `ctx.from` уже даёт бесплатно (без
      согласия юзера, публичные данные): `id`, `username`, `first_name`, `last_name`,
      `language_code`. Сейчас `collect` пишет только то, что явно собрано через `input`-узлы —
      можно подмешивать эти поля автоматически в каждую запись `leads.jsonl`.
- [ ] **`request_contact`-узел** — для реального номера телефона нужен отдельный DSL-узел с
      Telegram-кнопкой "Поделиться контактом" (`request_contact: true`) — в отличие от профиля
      выше, это требует явного согласия пользователя (нажатие кнопки), не читается автоматически.
- [ ] **Realtime-обновление дашборда** — сейчас `/bots/:id` рендерится один раз при заходе,
      данные не обновляются без перезагрузки страницы (уже путало — "No collected data yet" на
      самом деле было просто устаревшей страницей). Когда будет реальная БД (см. "Потом — обвязка
      сервиса"), взять что-то с realtime-подпиской (Firebase RTDB/Firestore, Supabase realtime)
      вместо файлового JSON, чтобы дашборд обновлялся сам.

## Дальше — пробелы относительно конкурентов (ManyChat / SendPulse / BotHelp)

- [x] **`condition`-узел** — ветвление по значению собранных данных (`field`, опционально `equals`,
      `if_true`/`if_false`). Обнаружено реальным использованием: AI-чат пытался построить paywall
      ("если уже куплено — сразу приватный контент, иначе — оплата") и не смог, в DSL не было
      ветвления. Заодно `payment`-узел теперь автоматически пишет `state.data[payload] = "true"`
      при успешной оплате — `condition` может проверить именно это поле, не нужен отдельный шаг
      "запомнить покупку". Задокументировано в `dsl-rules.md` с готовым паттерном пэйвола.
      Добавлено во все места, что знают про типы узлов: `schema.ts`, `engine.ts`, `flow-check.ts`,
      `flow-to-reactflow.ts`, `mermaid.ts`, форма узла в `views.ts`.
- [x] **CSS-баг: поле текста кнопки было невидимым** — `select` (next) не имел ограничения ширины
      и забирал себе почти всю строку через общее правило `width:100%`, из-за чего инпут текста
      кнопки сжимался в полоску толщиной в пиксель. Обнаружено живым использованием ("не понимаю
      как кнопки создавать"). Починено (`select` теперь `flex:0 0 auto; min-width:140px`) +
      добавлены подсказки в форме кнопок и медиа (обязательность URL).
- [x] **Загрузка медиа-файлов напрямую (без публичного URL)** — обнаружено вопросом: "сервер же у
      меня локальный, а медиа просит https". Решение — `InputFile` из grammy: бот сам стучится к
      Telegram и заливает байты (multipart), Telegram ничего не запрашивает обратно — значит
      публичный URL вообще не нужен. `media.url` теперь либо `http(s)://` (Telegram сам скачивает),
      либо голое имя файла из `bots/<id>/media/` (бот заливает сам). Кнопка загрузки в форме узла →
      `POST /bots/:id/media` → сохраняет в `bots/<id>/media/`. Проверено: реальным чтением файла
      через `InputFile.toRaw()` и живым HTTP-запросом на upload-роут.
- [x] **Создание бота из промпта, а не ручного YAML** — форма "Create a bot" теперь: описание
      бота словами → "✨ Generate flow.yaml" → `aiGenerateFlow()` (та же валидация+ретрай, что и у
      редактирования) заполняет поле flow.yaml, можно поправить руками перед стартом. Закрывает
      изначальную идею "юзер пишет в AI что хочет → получает YAML" прямо в дашборде, без похода во
      внешний чат. Проверено реальным вызовом (FAQ-бот с 2 темами и Back-навигацией, с первой
      попытки прошёл валидацию).
- [ ] **Предупреждение о несохранённом черновике в панели узла** — обнаружено багом: правки в
      форме/AI-чате живут в черновике, пока не нажата "Save" именно на этой панели; переключение на
      другой узел до сохранения молча теряет правку. Нужно хотя бы предупреждать перед потерей.
- [ ] **Глобальные команды** — верхнеуровневый `commands:` (например `cancel → welcome`,
      `human → handoff`), которые срабатывают из любой точки флоу, не переписывая каждый узел.
      Прямой ответ на то, почему в ManyChat/SendPulse юзеры застревают в сценариях.
- [x] **Медиа в `message`/`menu`** — поле `media: {kind: photo|video|audio|document|voice, url}`,
      `text` становится подписью. Прокинуто через весь стек: `schema.ts` → `engine.ts`
      (`FlowResult.media`) → `telegram-adapter.ts` (диспетчеризация на `replyWithPhoto`/`Video`/
      `Audio`/`Document`/`Voice`) → форма узла, `flow-to-reactflow.ts`, `mermaid.ts`, `test-run.ts`,
      `dsl-rules.md`. Проверено смоук-тестом от схемы до диспетча на нужный метод Bot API.
- [x] **Паттерн "записаться на..."** — не потребовал нового узла: уже выражается `input`×N →
      `collect`/`webhook`. Задокументирован в `dsl-rules.md` как готовый пример (без реального
      календаря/динамических слотов — собираем предпочтение текстом, подтверждает человек/внешняя
      система на другом конце вебхука).
- [ ] **Расписание / периодическая рассылка** — САМАЯ тяжёлая из отложенных задач, ровно та
      "настоящая параллельность" из анализа конкурентов выше. Нужно: (1) персистентный список всех
      юзеров бота — сейчас `FlowEngine.state` только для активных сессий, нет "все, кто хоть раз
      нажал /start"; (2) планировщик вне request/response цикла (cron/interval per-bot в
      `BotManager`); (3) push-отправка напрямую через `bot.api.sendMessage`/`sendPhoto` и т.д.,
      минуя `execute()`. `dsl-rules.md` явно предупреждён не притворяться, что это уже работает.
      Отдельная архитектурная задача, не встраивается в текущий DSL "заодно".
- [ ] **`delay`-узел** — пауза перед следующим сообщением, для естественности диалога.
- [ ] **`tag` / `set_field`-узел** — пометить профиль пользователя (сегментация), отдельно от
      `collect` (это запись лида, а не тег).
- [ ] **Отложенные напоминания** ("не ответил за N часов → напомнить") — настоящая
      "параллельность": нужен планировщик вне запрос-ответного цикла (cron/таймер), а не узел в
      DSL. Самая тяжёлая по инфраструктуре задача в списке — отдельная фаза.

## Потом — прод на Deno Deploy

Целевая платформа — классический Deno Deploy, подтверждено пользователем (деплой через
`deployctl` из терминала). Ключевое решение: **редактирование только локально, прод — read-only
деплой готовых `flow.yaml`** (идея пользователя, аналогия с тем, как GraphQL-плейграунд прячут в
проде).

- [x] **`server-prod.ts`** — отдельный тонкий entrypoint, без единого `/bots`-роута дашборда
      вообще (не спрятаны условно — их просто нет в файле). Конфиг ботов через env
      (`PROD_BOT_IDS`, `BOT_TOKEN_<id>`, `PUBLIC_URL`, опционально `WEBHOOK_SECRET`), `flow.yaml`
      читается как обычный ассет деплоя. Проверено локально: валидация env (понятные ошибки),
      чтение `flow.yaml`, сборка `FlowEngine`+бота, падает ровно на реальном вызове Telegram с
      фейковым токеном (`bot.init()` требует валидный токен — так и должно быть).
- [x] **Вебхуки вместо long polling** — `webhookCallback(bot, "std/http")` из grammy, `Deno.serve`
      маршрутизирует `POST /webhook/:id`. Важная деталь, которую легко упустить: `bot.init()`
      нужно вызвать явно перед первым апдейтом — в polling-режиме (`bot.start()`) это скрыто
      внутри, в webhook-режиме `handleUpdate()` иначе падает с "Bot not initialized!".
- [x] **`collect` в проде — Deno KV, не файл** — `store.ts`: `saveLead(dataDir, ...)` при
      `dataDir` вида `"kv:<id>"` пишет в `Deno.openKv()` вместо диска (эфемерного на Deploy).
      `engine.ts` не менялся — просто получает другую строку. Проверено локально записью+чтением
      через реальный `Deno.openKv()`.
- [x] **Живой деплой выполнен и проверен** — не `deployctl` (отдельный пакет), а `deno deploy`,
      встроенный в сам Deno CLI 2.7.14 — новый продукт с `sandbox`/`database` подкомандами, другой
      набор флагов. По пути споткнулись дважды: (1) `--entrypoint` через `deno deploy create`
      почему-то не сохранился — пофиксили руками в "Edit app configuration" в дашборде; (2) без
      флага `--prod` деплой уходит только в preview (`<app>-<hash>.<org>.deno.net`), стабильный
      домен (`<app>.<org>.deno.net`) не обновляется — нужно `deno deploy --prod`. Бот отвечает на
      `/start` через стабильный домен, `getWebhookInfo` подтверждает регистрацию.
- [ ] **State пользователя (`FlowEngine.state`) не переживает между изолятами** — если юзер
      посреди диалога (ждём ответ на `input`), а следующий вебхук-запрос попадёт в другой
      изолят — состояние потеряно. **Не решено** (в отличие от `collect`, здесь пока просто `Map`
      в памяти `server-prod.ts`, как и в dev). Нужен тот же переезд на Deno KV, что уже сделан для
      лидов, но для сессионного состояния — отдельная задача, `FlowEngine` придётся научить читать
      состояние асинхронно, сейчас `state.get()`/`state.set()` синхронные.
- [ ] Очередь на исходящие сообщения с троттлингом (лимиты Telegram: ~30/сек общий, 1/сек на чат)
- [ ] Медиа в проде — если "деплоим как есть" не годится для больших файлов (лимит Deploy на
      деплой ~1GB суммарно), тогда S3-совместимое хранилище (R2/S3/B2) с публичным URL, `media.url`
      просто `http(s)://`, `InputFile` не нужен вообще

## Потом — обвязка сервиса (сознательно отложено, уже после прод-архитектуры выше)

- [ ] Auth пользователей
- [ ] Реестр ботов (несколько токенов, несколько flow на аккаунт)
- [ ] Multi-tenant диспетчер вебхуков вместо long polling на процесс
- [ ] Горизонтальное масштабирование app-серверов за балансировщиком (когда одного инстанса мало)

`engine.ts` менять не придётся — вся Telegram-специфика в `telegram-adapter.ts`/`bot-manager.ts`.

## Уже закрыто

- [x] Движок: `message`, `menu`, `input`, `webhook`, `collect`, `payment` — схема (`schema.ts`),
      валидатор (`validate.ts`), исполнение (`engine.ts`)
- [x] Реальный внешний вызов — `webhook`-узел, проверено на локальном моке (`mock-crm.ts`)
- [x] Внутренний сбор данных — `collect`-узел, пишет в `data/leads.jsonl`
- [x] Оплата — `payment`-узел через Telegram Stars (XTR), без внешнего провайдера
- [x] `dsl-rules.md` — документ-правила для генерации `flow.yaml` любой LLM
- [x] Аудит мёртвого кода перед опенсорсом — реального мёртвого кода почти не нашлось; удалён
      `mermaid.ts` (полностью вытеснен `flow-to-reactflow.ts`+дашбордом, нигде не импортировался);
      запинены версии `npm:js-yaml`/`npm:zod`; `deno lint` теперь чист по всему проекту
